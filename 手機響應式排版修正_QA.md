# 留白陶所｜手機響應式排版修正 QA

修正日期：2026-07-24  
公開網址：https://gavin1424.github.io/liubai-pottery-studio-demo/  
修正前基準：`15fd9760ced0c5c65d77cfdd4e5e13cca95fe455`  
可回復標籤：`before-mobile-responsive-fix-20260724`

## 問題原因

1. 導覽列在 `max-width: 1024px` 已切換為漢堡選單，但 Hero 原本要到 `max-width: 768px` 才改成單欄，因此介於兩者的 viewport 會出現「漢堡選單＋桌面雙欄 Hero」。
2. Hero 桌面圖片使用固定最小高度，狹窄欄位仍沿用桌面裁切，造成花瓶圖變成過窄直欄。
3. H1 只用 `<br>` 控制桌面換行，欄寬縮小時「器物」可能被拆開。
4. 部分 Grid/Flex 子項未明確設定 `min-width: 0`，在長文字或較窄容器中有阻止縮小的風險。
5. 關於區塊在平板規則中把圖片跨滿欄，但 768px 以下沒有完整重置標題、內文與圖片的節奏。

所有 HTML 頁面原本均已具有正確 viewport meta。本次沒有使用 `overflow-x: hidden` 掩蓋問題，而是直接修正斷點、欄位與內容尺寸。

## 完成的修正

- 769px 以上保留 Hero 左文案、右圖片雙欄。
- 768px 以下與實體裝置寬度 768px 以下，Hero 改為單欄：文案、按鈕、三項特色、圖片。
- H1 改為兩個語意行並保護「器物」不拆字；手機使用 `clamp(2rem, 9vw, 3rem)`、1.3 行高。
- Hero 圖在手機使用 4:3、`object-fit: cover`、61–62% 水平焦點；未新增或替換原圖。
- 關於區塊在手機改為標題、理念、詳細介紹、連結、圖片的完整單欄，區塊 padding 72px。
- 390px／360px：課程單欄、作品兩欄、流程單欄、學員內容單欄、FAQ 滿寬、Footer 兩欄。
- 768px：課程與流程兩欄、Footer 三欄。
- 1024px／1440px：課程四欄、流程五步橫向、Hero 雙欄。
- 手機固定 CTA 僅在離開 Hero 後顯示，接近 Footer 時隱藏。
- CSS 與 JavaScript 加入版本查詢字串，避免 GitHub Pages 舊快取。

## 公開版本 viewport 測試

| Viewport | Hero | 標題 | 課程／流程 | 橫向溢位 | 結果 |
|---|---|---|---|---|---|
| 360×800 | 單欄、圖片 4:3 | 3 行，「器物」完整 | 1 欄／1 欄 | 無 | 通過 |
| 390×844 | 單欄、圖片 4:3 | 3 行，「器物」完整 | 1 欄／1 欄 | 無 | 通過 |
| 412×915 | 單欄、圖片 4:3 | 3 行，「器物」完整 | 1 欄／1 欄 | 無 | 通過 |
| 768×1024 | 單欄、圖片 4:3 | 2 行 | 2 欄／2 欄 | 無 | 通過 |
| 1024×768 | 左右雙欄 | 2 行 | 4 欄／5 步橫向 | 無 | 通過 |
| 1440×900 | 左右雙欄 | 2 行 | 4 欄／5 步橫向 | 無 | 通過 |

六個 viewport 均以瀏覽器設定真實 viewport 測試；`documentElement.scrollWidth` 與 `clientWidth` 相同。

## 功能與互動

- 手機漢堡選單：可開啟、可關閉，點擊錨點後自動收合。
- Hero CTA 與所有錨點：可操作。
- 手機固定 CTA：中段顯示、Footer 前隱藏，不遮擋首屏與頁尾。
- FAQ：可展開／收合；390px 點擊區 64px 高，內容未超出 viewport。
- 預約表單：空白送出顯示 7 個欄位錯誤；填入測試資料後顯示「資料未送出、未儲存」的示範完成提示。
- 圖片：完整捲動後破圖數 0；所有正式圖片與圖示均使用本地相對路徑。
- Console error：0。

## 公開資源

- 首頁、`privacy.html`、`terms.html`、`404.html`：HTTP 200。
- `styles.css?v=20260724-mobile-2`：HTTP 200。
- `script.js?v=20260724-mobile-2`：HTTP 200。
- favicon、OG 圖及 11 張頁面 WebP 圖片：HTTP 200。
- `robots` meta：`noindex, follow`。
- canonical：維持原 GitHub Pages 公開網址。

## 截圖

- `留白陶所_修正後_360px.png`
- `留白陶所_修正後_390px.png`
- `留白陶所_修正後_768px.png`
- `留白陶所_修正後_1440px.png`

截圖為本機 QA 證據，已由 `.gitignore` 排除，不會增加公開網站載入量。

## 已知限制

- 手機瀏覽器強制「電腦版網站」時，瀏覽器會自行改變 layout viewport；本次另以 `max-device-width` 補強，使實體手機仍套用單欄 Hero。主要驗收仍以一般手機模式的真實 viewport 為準。
- 固定 CTA 僅在 520px 以下啟用；平板沿用頁面內 CTA。

## 結論

本次手機響應式排版、互動、圖片與公開資源檢查均通過，沒有剩餘阻塞。
